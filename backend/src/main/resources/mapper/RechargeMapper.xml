<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.RechargeMapper">
    <resultMap id="RechargeResultMap" type="com.gym.entity.Recharge">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="amount" column="amount"/>
        <result property="type" column="type"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="userName" column="user_name"/>
    </resultMap>
    
    <select id="findAll" resultMap="RechargeResultMap">
        SELECT r.*, u.name as user_name
        FROM recharge r
        LEFT JOIN user u ON r.user_id = u.id
        ORDER BY r.create_time DESC
    </select>
    <select id="findByUserId" resultMap="RechargeResultMap">
        SELECT r.*, u.name as user_name
        FROM recharge r
        LEFT JOIN user u ON r.user_id = u.id
        WHERE r.user_id = #{userId}
        ORDER BY r.create_time DESC
    </select>
    <insert id="insert">
        INSERT INTO recharge (user_id, amount, type, remark)
        VALUES (#{userId}, #{amount}, #{type}, #{remark})
    </insert>
</mapper>

