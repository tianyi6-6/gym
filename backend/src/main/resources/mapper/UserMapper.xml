<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.UserMapper">
    <select id="findByUsername" resultType="com.gym.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <select id="findById" resultType="com.gym.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <select id="findAll" resultType="com.gym.entity.User">
        SELECT * FROM user ORDER BY create_time DESC
    </select>
    <insert id="insert">
        INSERT INTO user (username, password, name, phone, email, avatar, balance, member_level, status)
        VALUES (#{username}, #{password}, #{name}, #{phone}, #{email}, #{avatar}, #{balance}, #{memberLevel}, #{status})
    </insert>
    <update id="update">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            <if test="password != null">password = #{password},</if>
        </set>
        WHERE id = #{id}
    </update>
    <update id="updateBalance">
        UPDATE user SET balance = balance + #{amount} WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>

